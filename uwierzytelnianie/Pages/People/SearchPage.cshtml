@page
@model uwierzytelnianie.Pages.People.SearchPageModel
@{
    ViewData["Title"] = "SearchPage";
}

<form method="get">
    <div class="input-group">
        <label asp-for="NameTerm">Imie</label>
        <input class="form_control" asp-for="NameTerm" /> <br/>
        <label asp-for="SurnameTerm">Nazwisko</label>
         <input class="form_control" asp-for="SurnameTerm" />
        <div class="input-group-append">
            <button type="submit" class="btn btn-primary">Szukaj</button>
        </div>
    </div>
</form>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Person.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Person.Surname)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Person.Year)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Person.DateTime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Person.IsLeap)
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.People) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Surname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Year)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsLeap)
            </td>
            <td>
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>